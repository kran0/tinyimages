---
name: 'build and deploy images'

on:
  push:
    branches:
      - master
  schedule:
    - cron: '15 4 * * sun,tue,thu,sat'

jobs:
  alpine-based:
    runs-on: ubuntu-latest
    strategy:
      max-parallel: 2
      matrix:
        alpine-version: [ edge, latest ]
        name: [ socat, tor, svn ]
        include:

          - name: socat
            packages: socat so:libncursesw.so.6
            entrypoint: '[ "/usr/bin/socat" ]'
            command: '[ "-h" ]'

          - name: tor
            packages: tor
            entrypoint: /usr/bin/tor
            command: -h

#          - name: openjdk8-jre
#            packages: openjdk8-jre so:openjdk8:libjli.so
#            entrypoint: /usr/lib/jvm/java-1.8-openjdk/jre/bin/java
#            command: -h
#ENV PATH /usr/lib/jvm/java-1.8-openjdk/bin/

          - name: svn
            packages: subversion
            entrypoint: /usr/bin/svn
            command: -h

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Prepare
        id: prep
        run: |
         DOCKER_IMAGE=${{ secrets.DOCKERHUB_USER }}/tiny

         case "${{ matrix.alpine-version }}" in
          'edge') TAG="${DOCKER_IMAGE}:${{matrix.name}}-edge" ;;
          *     ) TAG="${DOCKER_IMAGE}:${{matrix.name}}"      ;;
         esac

         DOCKERFILE=$(mktemp --tmpdir=${HOME}) # $HOME are persisted between actions in the same job?
         (
         cat ./apkbased/Dockerfile-alpine
         cat <<DOCKERFILE
         ENTRYPOINT ${{matrix.entrypoint}}
         CMD  ${{matrix.command}}
         DOCKERFILE
         ) > "${DOCKERFILE}"

         # Set output parameters.
         echo ::set-output name=tag::${TAG}
         echo ::set-output name=dockerfile::${DOCKERFILE}

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v2
        with:
          platforms: all

      - name: Set up Docker Buildx
        id: buildx
        uses: docker/setup-buildx-action@v2

      - name: Login to DockerHub
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USER }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build
        uses: docker/build-push-action@v3
        with:
          builder: ${{ steps.buildx.outputs.name }}
          context: "{{defaultContext}}:apkbased"
          file: ${{ steps.prep.outputs.dockerfile }}
          platforms: linux/386,linux/amd64,linux/arm/v6,linux/arm/v7,linux/arm64/v8,linux/ppc64le,linux/s390x
          push: true
          tags: ${{ steps.prep.outputs.tag }}
          build-args: |
            ALPINE_VERSION=${{ matrix.alpine-version }}
            PACKAGES=${{ matrix.packages }}
            ADDFILES=${{ matrix.addfiles }}
            ENTRYPOINT=${{ matrix.entrypoint }}
            COMMAND=${{ matrix.command }}

      - name: Test default arch
        env:
          COMPOSE_FILE: ./apkbased/compose-sut.yaml
          IMAGE_NAME: ${{ steps.prep.outputs.tag }}
        run: |
         set -e
         docker-compose up sut
         docker-compose down
